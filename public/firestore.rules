rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access for development purposes.
    // WARNING: This is insecure for production environments.
    // You should implement more granular rules before going live.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
